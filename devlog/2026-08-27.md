# 开发日志 - 2026-08-27

## 已完成

- 恢复生产构建验证，TypeScript 编译与 Vite 构建均通过。
- 统一 CAD 导入内部单位为米，并在界面显示图纸原始单位。
- 修复 DWG WASM 文件加载路径与版本不匹配导致的导入失败。
- 完成刚性车自动安全路线 V1：单侧安全距离 0.5m，允许在道路横向范围内自动选线。
- 自动路线使用完整车身轮廓检查，并按 0.2m 步长复核；无安全路线时明确提示首次失败里程。
- 画布新增紫色虚线的自动路线显示，模拟面板显示路线最小净距。
- 无安全路线时改为继续完成全程模拟，保留回放、扫掠包络和红色冲突点，并提示首次复核失败里程。
- 自动选点改为按上一姿态和最小转弯半径计算可达扇形，候选点不可达时不再以折线突变强行连接。
- 规划改用 2m 纵向站距、0.1m 横向网格；相邻状态输出为首尾相切圆弧，支持在入弯前提前布置横向位置。
- 自动路线在最终净距复核失败时仍保留候选路径并标注实际冲突，界面区分“自动路线复核失败”和“中线回退轨迹”。
- 已用 `test 文件/test-1.dwg` 和 `test 文件/test-2.dwg` 验证导入与路线复核流程。
- 按前轴转向车辆的实际控制方式重构刚性车路径参考点：紫色路线现在是前转向轴中心轨迹，不再错误地表示最后轴轨迹。
- 新增前轴轨迹→车辆位姿的 Ackermann 反算：规划、逐步回放、车身角点、扫掠包络及碰撞检测使用同一参考定义；最后轴仍作为 `VehiclePose.position` 保存。
- 画布增加紫色前转向轴圆点，便于直接核对该点是否落在紫色路线。
- 使用 `test 文件/test-1.dwg` 完成导入和全程模拟验证（15.1m × 3.0m 车辆）：图例和车辆前轴标识正确显示；在 340.0m 处没有满足 0.5m 净距的位置，系统保留全程中线回退轨迹并标出 909 处实际碰线。
- 修复前轴路径接缝导致的后轴漂移：删除“原地换向”帧，车辆改由上一帧后轴连续滚动推进，前轮转角以每米 4° 的上限平滑变化。
- 新增最小回归场景（50m 直线接 20m 圆弧），`npm run test` 验证 409 帧中后轴相邻帧最大位移为 0.200m。
- 再次使用 `test 文件/test-1.dwg` 验证：原先接缝处的红色扇形发散消失；物理连续轨迹下记录 1272 处实际碰线，说明该图纸在 0.5m 净距要求下仍不可通行。

## 验证结论

- 两份测试 DWG 均可正常导入并识别为三图层道路。
- 对当前 15.1m × 3.0m 车辆及 0.5m 单侧净距，两份图纸在约 342.8m 处均未找到连续安全路线；系统正确给出不可通行提示，并继续显示全程轨迹和实际碰线位置。
- 采用最大转向角可达性约束后，`test-1.dwg` 在约 335.2m 处判定后续候选不可达，画布不再出现自动路线突变。
- 全程圆弧规划可越过原 335.2m 的不可达点；当前车辆在约 390.0m 的 0.2m 净距复核仍不通过，但冲突数降至 976，且候选自动路线连续可见。

## 待办事项

- 请使用实际项目车辆参数与图纸验证自动路线的工程合理性。
- 继续前先完善可转弯性与连续曲线路线的运动学复核，再进入 DXF 导出阶段。
